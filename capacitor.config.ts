import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kaunstdadenga.toolbarbug',
  appName: 'ToolbarBug',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
